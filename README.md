# LocalOCR

Offline Android OCR using Qwen2-VL-2B + LiteRT-LM.

- Arabic + English exact-text prompt
- Gallery + Camera
- GPU first, CPU fallback
- Copy + Share
- Model downloads once (~1.78 GB); inference is offline afterwards

APK is built by GitHub Actions.
